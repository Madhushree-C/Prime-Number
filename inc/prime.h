/**
* @file prime.h
*
*/
#ifndef __PRIME_H__
#define __PRIME_H__

/**
* Calculates the wheather the given number is prime number or not
* @param[in] performs the condition specified
* @return 1 or 0 based on the input
* @note Returns -1 for negative values
*/
int prime(int number);

#endif /* #ifndef __PRIME_H__ */
